import { PlaceholderPage } from '@/components/PlaceholderPage'

export default function DashboardPage() {
  return (
    <PlaceholderPage
      title="工作台 · Dashboard"
      milestone="M-SN-2 起开始填充关键运营指标卡片"
      note={<p style={{ margin: 0, color: 'var(--fg-muted)' }}>Hello server-next</p>}
    />
  )
}
