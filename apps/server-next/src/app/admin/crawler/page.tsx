import { PlaceholderPage } from '@/components/PlaceholderPage'

export default function CrawlerPage() {
  return <PlaceholderPage title="采集控制" milestone="M-SN-6（站点行展开 + MACCMS 配置 + 线路别名分组 + 任务依赖 DAG，参见 reference §5.6；DAG 待 reference 待明确项 A2 确认后落地）" />
}
