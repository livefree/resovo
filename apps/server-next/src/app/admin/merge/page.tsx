import { PlaceholderPage } from '@/components/PlaceholderPage'

export default function MergePage() {
  return <PlaceholderPage title="合并拆分" milestone="M-SN-5（依赖 split-unmerge / candidate-preview API + ADR-105）" />
}
